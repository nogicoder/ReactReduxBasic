import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app/config/app';


// Register the main App component
AppRegistry.registerComponent(appName, () => App);